key commands:
git clone "github url here"
git add .
git commit -m "msg"
git push
git pull

extra commands:
git status
git log
git revert 'commit number here'


git config --global user.name "Your Name"
git config --global user.email
git reset --hard

git checkout           this will help switch branches
git branch -a          show all branches
git switch "branch name"
